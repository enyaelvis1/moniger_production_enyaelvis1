# Local Edge Function secrets only. Never commit the real .env file.
# `supabase start` supplies SUPABASE_URL and Supabase keys automatically.
APP_BASE_URL=http://localhost:8080
PAYSTACK_SECRET_KEY=sk_test_replace-me
